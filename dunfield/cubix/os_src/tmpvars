		CUBIX Temporary Variable Usage

Temp:	CALFRE (current sector)
	GETHEX (DIGIT, SHIFT COUNT)
	TLOOK (MIN SIZE)
	FRESEC (Calculated size of disk)
	SSR HANDLER
	HDE (Disk error code)

Temp1:	MULTIPLE DELETES
	SHOW MEMORY (ADDR)
	CALFRE (sector count)
	COMFIL (IMPLIED RUN FLAG)
	CREATE (ALLOCATED SECTOR)
	EXE (IMPLIED RUN FLAG)
	EXEGO (EXECUTE ADDRESS)
	GETSNA (IMPLIED RUN FLAG)

Temp2:	SAVE  (Current sector count)
	CONNECT (Device ID)
	DOWNLOAD (Device ID)
	CALFRE (Size of disk)
	SAVED COMMAND POINTER

Temp3:	INITIALIZE (Link sector counts)
	SAVE & BSAVE (set # sectors)
	DOWNLOAD (Record count)

Temp4:	INITIALIZE (DIR ALLOC & INTERLEAVE)
	SAVE & BSAVE (SET LOAD ADDRESS)
	DOWNLOAD (LOW ADDRESS)
	CREATE (LOAD ADDRESS)
	SUSPEND (SAVED RETURN ADDRESS)
	RESUME (SAVED RETURN ADDRESS)
